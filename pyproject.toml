[project]
name = "aerial-robotics"
version = "0.1.0"
description = "Aerial robotics course — Webots simulation and exercises"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy",
    "scipy",
    "matplotlib",
    "pandas",
    "opencv-python",
    "jupyter>=1.1.1",
    "ipywidgets>=8.0.0",
]

[tool.uv]
package = false
